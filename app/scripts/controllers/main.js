'use strict';

/**
 * @ngdoc function
 * @name ngICardMarketApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the ngICardMarketApp
 */
angular.module('ngICardMarketApp')
  .controller('MainCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
